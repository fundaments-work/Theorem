[package]
name = "tauri-plugin-android-tts-audio"
version = "0.1.0"
description = "Native AudioTrack playback for TTS on Android."
authors = ["Fundamentals"]
license = "MIT"
edition = "2021"
links = "tauri-plugin-android-tts-audio"
build = "build.rs"

[dependencies]
tauri = { version = "2", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
tauri-plugin = { version = "2.4", features = ["build"] }
